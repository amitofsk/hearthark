export declare type ResponseToken = {
    accessToken: string;
    tokenType: string;
};
declare function generateBearerToken(credentialsFilePath: any): Promise<ResponseToken>;
declare function GenerateToken(credentialsFilePath: any): Promise<ResponseToken>;
declare function generateBearerTokenFromCreds(credentials: any): Promise<ResponseToken>;
export { generateBearerToken, GenerateToken, generateBearerTokenFromCreds };
